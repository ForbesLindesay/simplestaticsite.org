{
  "name": "simplestaticsite.org",
  "private": true,
  "description": "Simple instructions for setting up a static website",
  "dependencies": {
    "express": "^4.13.3"
  },
  "devDependencies": {
    "mandate": "^1.1.0",
    "rimraf": "^2.5.0",
    "stop": "^3.0.1"
  },
  "scripts": {
    "test": "echo No Tests",
    "predeploy": "rimraf out && node stop",
    "deploy": "mandate out",
    "postdeploy": "rimraf out"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ForbesLindesay/simplestaticsite.org.git"
  }
}
